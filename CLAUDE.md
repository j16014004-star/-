# AI Career Agent - 项目记忆文件

> **最后更新**: 2026-07-16
> **用途**: 快速恢复项目上下文

---

## 📋 项目简介

AI Career Agent 是智能求职助手平台前端，提供简历分析优化、岗位匹配推荐、AI 模拟面试、职业规划等功能。

**技术栈**: Vue 3 + TypeScript + Vite + Element Plus + Tailwind CSS 4 + Pinia  
**后端计划**: FastAPI + LangChain + JWT + MySQL + Vector DB

---

## 🚦 当前状态

- **前端框架**: ✅ 100% 完成
- **页面 UI**: ✅ 100% 完成（所有页面）
- **Mock 数据**: ✅ 100% 完成
- **测试**: ✅ 98/98 通过
- **开发服务器**: ✅ 端口 3000

### 最近完成
1. ✅ 简历管理去除“开始分析”，保留并美化 AI 优化入口，清理旧分析接口调用
2. ✅ 简历优化页接入异步优化任务，支持目标岗位下拉/自定义岗位、确认信息预览、AI 重写、手动补充、保存前确认
3. ✅ 简历管理新增“优化简历”标签页和优化简历详情页，用于展示、下载、删除已保存优化版本
4. ✅ 职业生涯规划页已接入真实后端链路：附件上传/删除、创建档案、创建规划任务、轮询 AI 任务、获取并展示规划结果
5. ✅ 新增/更新后端对接文档：`最新接口.md`、`职业生涯规划.md`、`简历优化确认接口.md`、`优化简历保存管理接口.md`
6. ✅ 品牌统一为“哈基米AI”，定位为“全自动找工作一站式自动化服务”，登录页使用 `public/hakimi-logo.png` 作为站点 Logo
7. ✅ 登录页首屏强化核心亮点：AI 做简历、自动投递、约面试、HR 沟通、职业规划
8. ✅ 单元测试当前通过：`13` 个测试文件、`97` 个测试用例通过
9. ⚠️ 全项目 `vue-tsc` 仍有其他模块既有类型错误，已多次定向确认本次简历/品牌相关改动未新增对应错误

---

## 📁 项目结构

```
ai-career-agent/
├── src/
│   ├── api/              # API 请求层（auth, resume, career, job, chat, agent, hr, interview）
│   ├── components/       # 组件库（layout/, common/, business/）
│   ├── composables/      # 组合式函数（useAuth, usePagination, useSSE, useUpload）
│   ├── mock/             # Mock 数据系统（9 个模块）
│   ├── router/           # 路由配置
│   ├── stores/           # Pinia 状态管理（app, user, resume, chat）
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数（request, storage, format）
│   └── views/            # 页面视图（auth, dashboard, resume, career, job, chat, agent, hr, interview, profile）
└── tests/                # 测试文件（98 个用例）
```

---

## 📏 核心规则

### 协作规则（必须遵守）
1. **不要自动 git commit** - 除非用户明确要求
2. **保持数据库结构简单** - 不要随意增加新表或重新设计
3. **使用中文** - 所有沟通使用中文
4. **修改前先分析** - 先排查原因，确认后再修改
5. **最小化修改** - 不要重构不相关的代码

### 代码规范
- TypeScript + Composition API (`<script setup>`)
- Element Plus + Tailwind CSS
- 避免 `any`，使用明确类型
- 复杂逻辑必须有注释
- 所有异步操作必须有错误处理

### 命令
- 启动: `npm run dev`（端口 3000）
- 测试: `npm run test`
- `/remember` - 读取 CLAUDE.md 恢复上下文

---

## 🐛 已知问题

**当前状态**: 无已知 Bug

**潜在风险**:
- Mock 初始化时序：异步加载可能在路由守卫之后完成（未发现实际问题）
- localStorage 数据残留：用户登出后数据残留（无影响）

---

## ⚠️ 重要调试经验：Mock 拦截器问题

### 问题描述（2026-07-14 已解决）
**症状**：
- 前端登录/注册页面无反应
- 浏览器控制台没有网络请求
- 即使后端 FastAPI 运行正常也无法连接

**根本原因**：
- `src/main.ts` 中加载了 `setupAuthMock()`，在 axios 请求拦截器层拦截了所有 `/auth/*` 请求
- Mock 拦截器在请求发出前就返回假数据，请求根本没有发送到后端
- 导致前端看起来"没有反应"，实际上是 mock 系统拦截了请求

**解决方案**：
```javascript
// src/main.ts - 禁用 auth mock
// import('./mock/auth').then(({ setupAuthMock }) => setupAuthMock())
```

### 🔍 排查清单（遇到类似问题时按顺序检查）

1. **检查浏览器 Network 标签**
   - 如果没有任何请求 → 很可能是 mock 拦截器问题
   - 如果有请求但失败 → 检查后端 API 或代理配置

2. **检查 `src/main.ts` 中的 mock 初始化**
   - 确认目标模块的 mock 是否被禁用
   - 登录注册模块（auth）应该禁用 mock，使用真实后端

3. **检查 `src/mock/index.ts` 的拦截器逻辑**
   - 拦截器会匹配已注册的 mock handler
   - 如果匹配，直接返回假数据，不发请求

4. **检查 API 字段名是否匹配**
   - Mock 返回的字段名可能与后端不一致
   - 例如：mock 返回 `token`，后端返回 `access_token`

### 📋 当前 Mock 状态
- ✅ Auth（登录/注册）：**已禁用** - 使用真实后端 API
- ✅ 简历上传/列表/详情：**已禁用简历 CRUD Mock** - 使用真实后端 API
- ⚙️ 简历 AI/职业规划：开发环境由 `VITE_ENABLE_AI_MOCK` 控制，默认启用；后端完成后设置为 `false`
- ⚠️ 聊天、Agent、HR、面试：**仍启用** - 使用 mock 数据

### 💡 后续开发建议
- 当需要连接真实后端时，禁用对应模块的 mock
- 保留 mock 系统用于演示和离线开发
- 切换时只需在 `main.ts` 中注释/取消注释对应的 `setupXxxMock()` 调用

---

## 📝 待开发功能

### 高优先级
1. 后端 API 集成（替换 Mock 数据）
2. 文件上传功能（简历、头像）
3. 实时通信（SSE、WebSocket）

### 中优先级
4. 数据库实现（Schema、迁移脚本）
5. 用户认证完善（Token 刷新、多设备管理）
6. 性能优化（懒加载、虚拟滚动）

---

## 2026-07-16 最新交接状态（以本节为准）

### 已完成

- 简历管理已删除“开始分析”入口，AI 优化入口已替换到原位置。
- 简历优化目标岗位支持固定选项、岗位推荐接口选项、搜索及用户自定义岗位。
- 简历优化支持逐条确认问题、AI 补全预览、手动补充、暂不处理、反馈后重新生成、保存前查看修改内容。
- 简历管理已增加优化简历列表、详情、下载与删除入口。
- 职业生涯规划页面已取消正式的本地模拟生成，接入真实接口流程：项目附件上传/删除 → 创建 profile → 创建 plan → 轮询 AI task → 获取 plan 结果。
- 品牌和登录页已统一为“哈基米AI”，Logo 为项目内静态资源，部署后不依赖开发电脑临时路径。
- 当前单元测试结果：13 个测试文件、97 个测试用例通过。

### 尚未完成/注意事项

- 后端已实现职业生涯规划接口和 Agent，但独立的“简历分析 Agent”尚未实现；现有的是简历规则解析和 `ResumeOptimizationAgent`。
- 全项目生产构建仍受登录、聊天、面试、岗位等旧模块 TypeScript 错误影响；简历和职业规划改动的定向检查、测试已通过。
- 当前工作区仍有未提交修改；不得把不相关文件一起提交。最近已推送的前端提交为 `830216e feat: update frontend resume flows and hakimi branding`，之后的职业规划和简历确认改动尚未全部推送。
- 职业规划接口文档位于项目根目录 `职业生涯规划接口.md`。
- AI/职业规划联调时确认 `VITE_ENABLE_AI_MOCK=false`，避免请求被 Mock 拦截。

### 职业规划确认与执行打卡（2026-07-16）

- 职业规划结果页新增“确认采用并开始执行”和“不满意，反馈后重新生成”。
- 反馈支持选择岗位方向、学习强度、学习路径、项目任务、求职安排，并填写具体意见交给后端 Agent。
- 主导航新增 `/career/check-in`“计划执行打卡”页面，展示总进度、连续打卡、今日任务、本周任务和最近记录。
- 今日任务支持完成打卡、暂缓、撤销和填写执行备注。
- 新增前端接口契约文档 `职业规划确认与打卡接口.md`；确认、重新生成、当前执行计划和任务打卡后端接口现已实现。
- 用户进入 `/career` 时会先查询当前执行计划，再加载对应的已确认职业规划；再次打开页面直接显示此前确认方案，不重新调用 AI。没有正在执行的计划时才保留首次生成流程。
- 后端确认操作会幂等生成每日/每周任务；确认新方案时暂停旧执行计划，重新生成保留原规划和用户反馈。
- 2026-07-16 验证：前端 97 项 Vitest 全部通过，`vite build` 成功；全量 `vue-tsc` 仍存在其他旧模块的历史错误，职业规划改动未新增对应错误。
- 打卡任务新增独立“AI 学习答疑”区域，支持提交问题、异步轮询 Agent 回答及查看该任务的历史问答；打卡备注与学习问题分开保存。
- 新增接口文档 `职业规划打卡AI答疑接口.md`；后端仍需实现问答记录接口并让通用 AI Task 支持 `career_plan_question`。
- 打卡页新增“当天任务完成”闭环：主动询问用户今天结束或提前解锁下一项，并展示提前任务数量与阶段进度。
- 新增 `/career/check-in/assessment/:id` 阶段考核页：答案本地自动保存、支持单选/多选/简答/代码题、异步 AI 评分、逐题解析、补强计划和进入下一阶段。
- 新增后端接口契约 `职业规划提前推进与阶段考核接口.md`；后端需增加提前推进、考核生成、提交评分、结果、补强和阶段流转接口。

---

## 🗄️ 数据库设计

### 用户认证模块（sql_desgin.md）
- **users** - 用户主表（含 role、is_deleted 逻辑删除）
- **refresh_tokens** - 刷新令牌表（仅存储 refresh token，access token 不持久化）
- **verification_codes** - 验证码表
- **login_logs** - 登录日志表（含 login_location）

### 完整设计（database_design.md）
25 张表，覆盖所有业务模块

### API 文档（backend_api.md）
14 个模块的完整 API 设计

---

## 💡 重要决策

1. **Mock 数据系统** - 前端独立开发，未来切换到真实 API
2. **Element Plus** - 官方维护，组件丰富
3. **Tailwind CSS** - 开发速度快，样式一致性好
4. **Vitest** - Vite 原生支持，速度快
5. **不重构现有代码** - 保持稳定性，只修复 Bug

---

**维护说明**: 此文件由 Claude AI Assistant 维护，每次重要变更后更新。
